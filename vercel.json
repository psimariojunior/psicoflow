{
  "framework": "nextjs",
  "buildCommand": "prisma migrate deploy 2>/dev/null || (prisma db push --accept-data-loss --skip-generate && prisma migrate resolve --applied 0_init 2>/dev/null); prisma generate && next build",
  "installCommand": "npm install",
  "outputDirectory": ".next"
}
